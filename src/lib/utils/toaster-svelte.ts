import { createToaster } from '@skeletonlabs/skeleton-svelte';

// Create a customized toaster with default settings
// It will use system styles that include fully opaque backgrounds
export const toaster = createToaster();